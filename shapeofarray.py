#shape of array
import numpy as np 
a = np.array([[1,2],[3,4],[5,6]], dtype = np.float64) # specify data type while creating an array
print(a.shape) # specify the shape of the array (rows and columns)
